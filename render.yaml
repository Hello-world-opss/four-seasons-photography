services:
  - type: web
    name: four-seasons-photography
    env: node
    region: singapore
    buildCommand: npm install
    startCommand: node server-cloud.js
    envVars:
      - key: NODE_VERSION
        value: 18
